# Language Pressure Evidence

This directory records concrete MNCS-language/runtime/tooling pressure discovered while implementing `mncs-index`.

See `docs/LANGUAGE_PRESSURE.md` and RFC 0006.

Do not pre-populate speculative gaps as bugs. The project may list expected pressure domains in documentation, but registry entries should be grounded in an attempted workload and evidence.
